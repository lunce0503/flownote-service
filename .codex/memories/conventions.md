# 컨벤션 메모리

- `yarn.lock`이 있는 JavaScript 하위 프로젝트에서는 `yarn`을 사용한다.
- `flownote-API/`에서는 `uv`를 사용한다.
- `flownote-server/`에서는 Gradle wrapper 명령을 사용한다.
- 커밋되는 프로젝트 하네스 파일에는 비밀값을 넣지 않는다.
- 엄격한 규칙으로 만들기 전에 안정적인 컨벤션을 이곳에 먼저 문서화한다.

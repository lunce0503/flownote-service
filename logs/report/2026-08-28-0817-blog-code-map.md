# Blog 코드맵 작성

## 변경

- Canvas 코드맵과 같은 `docs/design-docs/` 구조에 간단한 Blog 코드맵을 추가했다.
- 목록, BlockNote 편집기, 웹 API, FastAPI Gateway, Go Notes, PostgreSQL/S3 흐름을 정리했다.
- 설계 문서 색인에 Canvas와 Blog 코드맵을 함께 연결했다.

## 검증

- 문서에서 참조하는 주요 코드와 문서 경로가 모두 존재함을 확인했다.
- `git diff --check`: 성공.
- `REDIS_HOST_PORT=6380 docker compose up -d --build`: 전체 이미지 빌드와 백그라운드 기동 성공.
- 로컬 헬스체크: React 200, Gateway 200, Spring actuator `UP`.

## 배포

- Vercel project: `flownote-react`.
- Deployment ID: `dpl_Cmf1iC5D36zsmvxxocX24YV2crMa`.
- Production alias: `https://flownote-react.vercel.app`.
- 상태: `READY`.
- Railway: 백엔드 실행 코드와 API 계약이 변경되지 않아 재배포하지 않았다.

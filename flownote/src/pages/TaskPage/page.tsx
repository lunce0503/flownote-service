import { TaskWidget } from "../../widgets";

const TaskPage = () => {
    

    return (
        <main className="flex-1 w-full bg-[#242424]">
            <TaskWidget />
        </main>
    );
}

export default TaskPage;

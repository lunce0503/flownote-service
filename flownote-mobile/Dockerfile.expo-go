FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci && npm cache clean --force

COPY . .

ENV EXPO_NO_TELEMETRY=1
ENV EXPO_MAX_WORKERS=2

EXPOSE 8080

CMD ["node", "scripts/start-expo-go.mjs"]

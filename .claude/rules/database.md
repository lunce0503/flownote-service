# 데이터베이스 규칙

- 스키마 변경은 frontend, API, service 전반의 계약 변경으로 취급한다.
- 임시 스키마 수정보다 migration을 우선한다.
- 읽기/쓰기 경로, 인덱스, null 허용 여부, 기본값, rollback 영향을 확인한다.
- 운영 인증 정보, 덤프, 개인정보는 저장소 파일에 포함하지 않는다.
- PostgreSQL 쿼리는 파라미터화를 확인하고 문자열 연결 SQL을 피한다.
- identifier나 enum 성격의 값을 바꿀 때는 모든 하위 프로젝트에서 호출부를 검색한다.
